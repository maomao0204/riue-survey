# RIUE 问卷 · 纯小白上线操作清单

> 目标：在你自己的 Windows 电脑上，让别人**扫码填问卷 → 答案自动汇总 → 只有你输密码能看后台数据**。
> 全程**不需要 GitHub、不需要买服务器、不用懂代码**。

---

## 🎯 你最后能得到什么

- 一张**二维码 / 海报** → 客户手机扫码 → 进入「分步引导」问卷页，一题一题填，体验好。
- 客户填完点提交 → 答案**自动存进你电脑里的数据库**（不用手动收）。
- 你打开 `https://xxxx/admin` 输入密码 → 看到**实时汇总表 + 统计 + 一键导出 Excel(CSV) + 清空**。
- 客户**永远看不到**别人的答案，也没有任何"后台入口"，数据只有你能看。

---

## 📋 第一次做（一次性准备，约 10 分钟）

### 第 1 步：确认电脑上有 Python
- 按 `Win + R`，输入 `cmd` 回车，黑窗口里敲：`python --version`
- 如果显示 `Python 3.x.x` → 已有，跳过。
- 如果显示"不是内部或外部命令" → 去 https://www.python.org/downloads/ 下载安装，**安装时务必勾选 "Add Python to PATH"**（最下面那个小方框）。

### 第 2 步：装 cloudflared（免费隧道工具，让手机能访问你电脑）
1. 打开 https://github.com/cloudflare/cloudflared/releases
2. 找 `cloudflared-windows-amd64.exe`，点它下载。
3. 下载完，**重命名为 `cloudflared.exe`**。
4. 把它复制到 `C:\Windows` 文件夹里（这样系统能找到它）。
   - 如果提示"需要管理员权限"，点"继续/提供权限"即可。

### 第 3 步：设一个后台密码（重要！否则你进不去后台）
1. 在本项目文件夹里，右键 `start_survey.bat` → 选"编辑"（用记事本打开）。
2. 找到这一行：
   ```
   set "ADMIN_PASSWORD="
   ```
3. 在等号后面填你的密码，例如：
   ```
   set "ADMIN_PASSWORD=Riue@2026"
   ```
   （换成只有你知道的强密码，别用 123456）
4. 按 `Ctrl+S` 保存，关掉记事本。

---

## 🚀 每次要收集时（日常操作，约 2 分钟）

### 第 4 步：双击启动
- 双击 `start_survey.bat`（会变成一个**黑窗口**一直开着，别关它）。
- 你会看到两行提示：
  ```
  [1/2] 启动问卷后端 server.py  (端口 8787) ...
  [2/2] 通过 Cloudflare 隧道暴露公网地址（免费、无需账号）...
  ```

### 第 5 步：复制公网地址
- 等几秒，黑窗口里会出现一行类似：
  ```
  https://xxxx.trycloudflare.com
  ```
- **用鼠标选中这串地址 → 右键复制**（这就是你问卷的公网网址）。

### 第 6 步：让二维码指向这个地址
1. 在本项目文件夹的空白处，**按住 Shift + 右键** → 选"在此处打开命令窗口"或"在终端中打开"。
2. 粘贴并运行（把地址换成你刚才复制的）：
   ```
   python set_links.py https://xxxx.trycloudflare.com
   ```
3. 回车后，会重新生成 `RIUE问卷二维码.png` 和 `RIUE问卷海报_v5.svg`。

### 第 7 步：自己先扫一下测试
- 用手机扫新生成的 `RIUE问卷二维码.png`。
- 应该能打开问卷、填一两题、提交成功（显示"提交成功"页面）。
- 如果打不开 → 看下面"常见问题"。

### 第 8 步：看实时数据
- 在你自己的电脑浏览器（或手机）打开：`https://xxxx.trycloudflare.com/admin`
- 输入第 3 步设的密码 → 进入后台：
  - 看到所有人的提交明细表
  - 看到统计（年龄段、热门选品等）
  - 点"导出 CSV"可下载 Excel
  - 点"清空"可删除全部数据（谨慎）

---

## 🛑 收工 / 停止收集
- 直接**关闭那个黑窗口**（或按 `Ctrl+C` 再关）即可。
- 关掉后，二维码就失效了（客户扫不出）。下次要收集，从第 4 步重来。

---

## ⭐ 进阶（可选）：让地址固定不变，不用来回改二维码
> 上面的"quick tunnel"每次重启地址都会变，得重跑第 6 步。想要**一个地址永久不变**，做一次下面配置：

1. 注册一个免费 Cloudflare 账号：https://dash.cloudflare.com/sign-up
2. 在本项目文件夹打开命令窗口，依次运行：
   ```
   cloudflared tunnel login
   ```
   （会弹浏览器让你登录，登录后关掉网页）
   ```
   cloudflared tunnel create riue-survey
   ```
   （建一个叫 riue-survey 的隧道，记下名字）
3. 用记事本打开 `start_survey.bat`，把顶部：
   ```
   set "TUNNEL_NAME="
   ```
   改成：
   ```
   set "TUNNEL_NAME=riue-survey"
   ```
   保存。
4. 以后双击 `start_survey.bat`，黑窗口会打印一个**固定地址**（形如 `https://xxxx.cfargotunnel.com`），只跑一次第 6 步重指二维码，之后每次双击都是同一个地址。

---

## ❓ 常见问题

**Q：客户扫码显示"无法访问 / 打不开"？**
- 你的电脑关了 / 那个黑窗口关了 → 重新双击 `start_survey.bat`。
- 你电脑断了网 → 连上网络再试。
- 公网地址变了（quick tunnel 重启会变）→ 重跑第 5、6 步。

**Q：后台进不去 / 密码不对？**
- 确认第 3 步在 `start_survey.bat` 里填了 `ADMIN_PASSWORD=你的密码` 并保存。
- 地址要带 `/admin`，例如 `https://xxxx.trycloudflare.com/admin`。

**Q：数据存在哪？会丢吗？**
- 存在你电脑的 `riue_survey.db` 文件里。只要不删这个文件、电脑不坏，数据就在。
- 建议定期在后台点"导出 CSV"备份一份到电脑。

**Q：别人能看到别人的答案吗？**
- 不能。客户只能提交自己那份，没有任何"看汇总"的入口。汇总只有你（凭密码进 /admin）能看到。

**Q：收集期间电脑必须一直开着吗？**
- 是的。因为服务器就跑在你这台电脑上。收集完才能关。

---

## 📁 这个文件夹里各文件是干嘛的
| 文件 | 作用 |
|------|------|
| `start_survey.bat` | 一键启动（双击它就行） |
| `server.py` | 后端程序（自动运行，不用管） |
| `survey-dist/index.html` | 客户填的问卷页面 |
| `admin.html` | 你登录看数据的后台页面 |
| `set_links.py` | 改二维码/海报指向用的工具 |
| `RIUE问卷二维码.png` | 给客户扫的二维码 |
| `RIUE问卷海报_v5.svg` | 海报（可印刷） |
| `DEPLOY.md` | 更详细的技术部署说明（给进阶看） |
