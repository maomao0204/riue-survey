# RIUE 问卷 · 纯小白上线操作清单

> 目标：在你自己的 Windows 电脑上，让别人**扫码填问卷 → 答案自动汇总 → 只有你输密码能看后台数据**。
> 全程**不需要 GitHub、不需要买服务器、不用懂代码**。

---

## ⚡ 三件套：每天就点这两个（照着点就行）

| 时机 | 双击哪个文件 | 会发生什么 |
|------|--------------|------------|
| 开始收集 | **`投放问卷.bat`** | 自动起后端 → 起公网隧道 → 重指二维码/海报 → 弹出问卷页 + 二维码 + 海报 |
| 收工 | **`stop_survey.bat`**（或中文名 **`停止问卷.bat`**，两者一样） | 干净关掉隧道和后端，二维码随即失效 |
| 备用 | `start_tunnel.bat` | 只起隧道（不含后端/二维码），一般不用 |

**最简 3 步（记这个就行）：**
1. 双击 **`投放问卷.bat`** → 等约 10 秒 → 手机扫自动弹出的 `RIUE问卷二维码.png`。
2. 看数据：浏览器打开 `https://<地址>/admin`，密码见 `投放问卷.bat` 里的 `ADMIN_PASSWORD`（你已设为 `137997953@`）。
3. 收工双击 **`stop_survey.bat`**。

> 运行时只有**一个黑窗口**，别关它（关了隧道就断、二维码就失效）。想要固定公网地址可看文末"进阶"。

---

## 🎯 你最后能得到什么

- 一张**二维码 / 海报** → 客户手机扫码 → 进入「分步引导」问卷页，一题一题填，体验好。
- 客户填完点提交 → 答案**自动存进你电脑里的数据库**（不用手动收）。
- 你打开 `https://xxxx/admin` 输入密码 → 看到**实时汇总表 + 统计 + 一键导出 Excel(CSV) + 清空**。
- 客户**永远看不到**别人的答案，也没有任何"后台入口"，数据只有你能看。

---

## 📋 第一次做（一次性准备，约 10 分钟）

### 第 1 步：确认电脑上有 Python
- 按 `Win + R`，输入 `cmd` 回车，黑窗口里敲：`python --version`
- 如果显示 `Python 3.x.x` → 已有，跳过。
- 如果显示"不是内部或外部命令" → 去 https://www.python.org/downloads/ 下载安装，**安装时务必勾选 "Add Python to PATH"**（最下面那个小方框）。

### 第 2 步：不用下载任何东西（Windows 自带 SSH 即可）
- **你不需要装 cloudflared。** Windows 10/11 自带 OpenSSH，双击 `投放问卷.bat` 会自动用「系统 SSH + localhost.run」打通公网，零下载、零命令行。
- 想确认一下有没有 SSH：按 `Win + R` → 输入 `cmd` 回车 → 敲 `ssh -V`，只要显示出版本号（如 `OpenSSH_for_Windows_...`）就 OK。
- （可选）只有当你**想要"固定不变的公网地址"**时才需要 cloudflared；但它要从 GitHub 下载，国内常被墙。先跳过，用默认的 SSH 隧道即可（地址每次启动会变，但脚本会自动重指二维码，你无感）。

### 第 3 步：设一个后台密码（重要！否则你进不去后台）
1. 在本项目文件夹里，右键 `投放问卷.bat` → 选"编辑"（用记事本打开）。
2. 找到这一行：
   ```
   set "ADMIN_PASSWORD=137997953@"   （你已设置，也可改成你自己的）
   ```
3. 把 `riue2026` 换成你的密码，例如：
   ```
   set "ADMIN_PASSWORD=你的新密码"
   ```
   （换成只有你知道的强密码，别用 123456）
4. 按 `Ctrl+S` 保存，关掉记事本。

---

## 🚀 每次要收集时（日常操作，约 1 分钟，纯双击）

### 第 4 步：双击启动（全自动）
- 双击 `投放问卷.bat`（会弹出一个**黑窗口**：后端和隧道都在里面后台运行，**别关它**）。
- 等约 8 秒，脚本会自动：
  1. 起好问卷后端；
  2. 用系统 SSH 打通公网隧道（无需任何下载）；
  3. **自动把二维码/海报重指到本次地址并直接打开图片**。

### 第 5 步：自己先扫一下测试
- 用手机扫自动弹出的 `RIUE问卷二维码.png`（或海报 `RIUE问卷海报_v5.svg`）。
- 应该能打开问卷、填一两题、提交成功（显示"提交成功"页面）。
- 如果打不开 → 看下面"常见问题"。

### 第 6 步：看实时数据
- 黑窗口里会打印你的公网地址，形如 `https://xxxx.lhr.life`。
- 在自己电脑/手机浏览器打开：`https://xxxx.lhr.life/admin`
- 输入第 3 步设的密码 → 进入后台：
  - 看到所有人的提交明细表
  - 看到统计（年龄段、热门选品等）
  - 点"导出 CSV"可下载 Excel
  - 点"清空"可删除全部数据（谨慎）

---

## 🛑 收工 / 停止收集
- 双击 `stop_survey.bat`（或中文名 `停止问卷.bat`，两者相同）即可干净退出（黑窗口和隧道都会关）。
- 关掉后，二维码就失效了（客户扫不出）。下次要收集，从第 4 步重来。

---

## ⭐ 进阶（可选）：让地址固定不变，不用来回改二维码
> 默认的 SSH 隧道每次重启地址都会变（但脚本已自动重指二维码，你无感）。想要**一个地址永久不变**，需装 cloudflared：
> ⚠️ cloudflared 要从 GitHub 下载，国内常被墙。若打不开下载页，先用默认的 SSH 隧道即可。
> 若你能装上 cloudflared，双击一次 `setup_once.bat` 完成登录+建隧道，之后每天双击 `投放问卷.bat` 即为固定地址。

---

## ❓ 常见问题

**Q：客户扫码显示"无法访问 / 打不开"？**
- 你的电脑关了 / 黑窗口关了 → 重新双击 `投放问卷.bat`。
- 你电脑断了网 → 连上网络再试。
- 显示 "no tunnel here" → 隧道掉了，重新双击 `投放问卷.bat` 即可（会自动换新地址并重指二维码）。

**Q：后台进不去 / 密码不对？**
- 确认第 3 步在 `投放问卷.bat` 里填了 `ADMIN_PASSWORD=你的密码` 并保存。
- 地址要带 `/admin`，例如 `https://xxxx.lhr.life/admin`。

**Q：数据存在哪？会丢吗？**
- 存在你电脑的 `riue_survey.db` 文件里。只要不删这个文件、电脑不坏，数据就在。
- 建议定期在后台点"导出 CSV"备份一份到电脑。

**Q：别人能看到别人的答案吗？**
- 不能。客户只能提交自己那份，没有任何"看汇总"的入口。汇总只有你（凭密码进 /admin）能看到。

**Q：收集期间电脑必须一直开着吗？**
- 是的。因为服务器就跑在你这台电脑上。收集完才能关。

---

## 📁 这个文件夹里各文件是干嘛的
| 文件 | 作用 |
|------|------|
| `投放问卷.bat` | **一键投放（双击它就行）**：起后端 → 起隧道 → 重指二维码 → 开图 |
| `stop_survey.bat` / `停止问卷.bat` | 停止脚本（两者相同，双击干净退出） |
| `start_survey.bat` | `投放问卷.bat` 的兼容别名，双击效果一样 |
| `start_tunnel.bat` | 仅起隧道的备用脚本（一般不用） |
| `setup_once.bat` | 可选：想要固定公网地址才用（需能下 cloudflared） |
| `server.py` | 后端程序（自动运行，不用管） |
| `survey-dist/index.html` | 客户填的问卷页面 |
| `admin.html` | 你登录看数据的后台页面 |
| `set_links.py` | 改二维码/海报指向用的工具 |
| `RIUE问卷二维码.png` | 给客户扫的二维码 |
| `RIUE问卷海报_v5.svg` | 海报（可印刷） |
| `DEPLOY.md` | 更详细的技术部署说明（给进阶看） |
